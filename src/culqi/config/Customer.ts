export interface ItemInfo {
    description: string;
    order: string;
}

export interface CustomerInfo {
    firstName: string;
    lastName: string;
    address: string;
    addressCity: string;
    phone: string;
    email: string;
  };